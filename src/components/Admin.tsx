//import { useState, useEffect, useRef, useCallback } from "react";
//import { useUserContext, Axios, isLocalhost } from './Context';

export default function Admin() {
  return <div id="admin-app-outer-container"></div>;
}
